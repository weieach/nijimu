/** The water / ripple memory field — the previous default homescreen. */
export const MEMORY_FIELD_PATH = "/ripple";

/** The naming step of the create flow. Rendered by HomePage (see App.tsx), so
    that saving can turn the naming rim into the field's gallery in place. */
export const NAMING_PATH = "/record/name";
